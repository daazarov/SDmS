{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",

  "NServiceBus": {
    "ConnectionString": "host=localhost;username=guest;password=guest",
    "RabbitEndPoint": {
      "Name": "sdms.device-listener.host",
      "ErrorQueue": "error"
    }
  },

  "Mongo": {
    "ConnectionString": "mongodb://daazarov:zaq1%40WSX@localhost:27017", // If the username or password includes the at sign @,
                                                                       // colon :, slash /, or the percent sign % character, 
                                                                       // use percent encoding
    "DbName": "devices",

    "EnableCommandLogging": true
  }
}
